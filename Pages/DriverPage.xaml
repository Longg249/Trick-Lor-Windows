<Page x:Class="TrickLor.Pages.DriverPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:TrickLor.Pages"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
      mc:Ignorable="d"
      Background="{ThemeResource BrushBg}"
      Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="{ThemeResource BrushSidebar}"
                BorderBrush="{ThemeResource BrushBorder}" BorderThickness="0,0,0,1" Padding="32,20">
            <StackPanel>
                <TextBlock Style="{StaticResource PageTitle}" Text="🔌 Driver Info"/>
                <TextBlock Style="{StaticResource PageSubtitle}"
                           Text="Danh sách driver thiết bị — cảnh báo driver chưa ký hoặc có lỗi (WMI, có thể mất vài giây)"/>
            </StackPanel>
        </Border>

        <!-- CONTENT -->
        <Grid Grid.Row="1" Margin="32,24">
            <Border Style="{StaticResource Card}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Toolbar -->
                    <Border Grid.Row="0" Padding="16,12"
                            BorderBrush="{ThemeResource BrushBorder}" BorderThickness="0,0,0,1">
                        <StackPanel Orientation="Horizontal">
                            <Button Content="↺  Làm mới" Style="{StaticResource BtnGhost}"
                                    Click="Refresh_Click" Margin="0,0,8,0"/>
                            <TextBlock Text="Lọc:" VerticalAlignment="Center" Foreground="{ThemeResource BrushMuted}"
                                       FontSize="12" Margin="0,0,8,0"/>
                            <TextBox x:Name="TxtFilter" Style="{StaticResource InputBox}"
                                     Width="200" Height="32" VerticalAlignment="Center"
                                     TextChanged="Filter_Changed"/>
                        </StackPanel>
                    </Border>

                    <!-- Loading -->
                    <TextBlock x:Name="TxtLoading" Grid.Row="1" Text="⏳ Đang truy vấn WMI, vui lòng đợi..."
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="{ThemeResource BrushMuted}" FontSize="14" Visibility="Collapsed"/>

                    <!-- DataGrid -->
                    <controls:DataGrid x:Name="DgDrivers" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                              Background="Transparent" BorderThickness="0"
                              RowBackground="{ThemeResource BrushBg}"
                              AlternatingRowBackground="{ThemeResource BrushSubtle}"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="{ThemeResource BrushBorder}"
                              HeadersVisibility="Column"
                              CanUserReorderColumns="False"
                              SelectionMode="Single"
                              FontSize="12" Visibility="Collapsed">
                        <controls:DataGrid.Columns>
                            <controls:DataGridTextColumn Header="Tên thiết bị"   Binding="{Binding Name}"         Width="260"/>
                            <controls:DataGridTextColumn Header="Phiên bản"       Binding="{Binding Version}"      Width="120"/>
                            <controls:DataGridTextColumn Header="Nhà sản xuất"   Binding="{Binding Manufacturer}" Width="180"/>
                            <controls:DataGridTextColumn Header="Trạng thái"     Binding="{Binding Status}"       Width="90"/>
                            <controls:DataGridTextColumn Header="Đã ký"          Binding="{Binding SignedStr}"    Width="70"/>
                        </controls:DataGrid.Columns>
                    </controls:DataGrid>
                </Grid>
            </Border>
        </Grid>

        <!-- STATUS -->
        <Border Grid.Row="2" Background="{ThemeResource BrushSidebar}"
                BorderBrush="{ThemeResource BrushBorder}" BorderThickness="0,1,0,0" Padding="32,14">
            <TextBlock x:Name="TxtStatus" Text="Đang tải..."
                       Foreground="{ThemeResource BrushMuted}" FontSize="13"/>
        </Border>
    </Grid>
</Page>
